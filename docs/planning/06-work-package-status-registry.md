# ForecastIQ — Work-Package Status Registry

**Version**: 1.0
**Status**: Living document — updated at each work-package completion
**Authority**: `docs/planning/05-implementation-work-packages.md` (definitions); delivery reports (evidence)

State model: Not Started → Prototype Exists → Partially Implemented → Implementation Complete → Review Findings Open → Accepted. Side states: Blocked, Deferred.

---

## Registry

| WP | Name | State | Last updated | Evidence / notes |
|----|------|-------|--------------|------------------|
| 01 | Repository + dev env | Accepted (bootstrap) | 2026-07-22 | Repository Bootstrap final report; `make dev-up`, CI green |
| 02 | DB foundation | Accepted (bootstrap) | 2026-07-22 | Migrations 20260801000001..05; integration suite |
| 03 | Identity + workspace | **Accepted** (DRB full review 2026-07-23) | 2026-07-23 | Implemented on branch `feature/wp03-identity-workspace` (base: accepted WP-08 tip `9620f1d`). Migration `20260801000006_create_identity` adds the `user_role` enum, `users` + `api_keys` tables, and the deferred `audit_events.user_id` FK (see DR-03). Identity module: domain (User/APIKey/Role), ports (TokenVerifier + repositories), user service (JWKS verify → **provision-on-first-use** → login; role read from DB, never the token), API-key service (**argon2id**, plaintext shown once, list excludes hash, owner-only revoke, key auth), audit **reader** (recorder pre-existing). JWKS verifier adapter (`adapters/auth/jwks`) verifies **RS256 + ES256** with stdlib crypto (no new JWT dep), cached + rotation-tolerant. Dev-mode verifier (`adapters/auth/devauth`) is **build-tag excluded from release** (fails closed under `-tags release`). New config `FIQ_AUTH_JWKS_URL/ISSUER/AUDIENCE/DEV_MODE`. No HTTP routes (handlers land in WP-15/WP-19). Reports: `docs/reviews/work-packages/WP-03-implementation.md`, `WP-03-delivery-review.md`. **DRB full review 2026-07-23: ACCEPTED** — SHA identity verified (local == `git ls-remote origin` == CI head == `6e98a1c474e431f41205c6b4df8202acb7271612`), CI run **30005809876** (`pull_request`, head `6e98a1c`) **success** with all six mandatory jobs green (none skipped/cancelled); scope clean (no route/CI/existing-migration changes); no Critical/High/Medium. **DRB-WP03-001 (Low — auth-event audit omits client IP)** deferred to WP-19 (middleware owns request IP); non-blocking. DR-03 resolved. **Accepted Implementation SHA `6e98a1c`.** PR #5 open, ready to merge to `main`. |
| 04 | Location management | **Accepted** | 2026-07-23 | DRB-WP04-001..005 verified **RESOLVED** at re-review (advisory-lock dedup proven vs real PostgreSQL; fp-tolerant boundary; mandatory override reason; restricted status lifecycle; doc corrections). RR-001/RR-002 resolved and **green in CI**. Two deferred mandatory CI gates **RESOLVED** (CI-WP04-001 backend-checks, CI-WP04-002 security). **Confirmatory re-review 2026-07-23: ACCEPTED** — all six mandatory CI jobs green on `15b8faa` (run 29952834878); commit identity, remote SHA, and CI headSha independently verified; no regression. Report: `docs/reviews/work-packages/WP-04-delivery-re-review.md` (§A, §B). |
| 05 | Adapter framework | **Accepted** (confirmatory re-review 2026-07-23; TC-05-01 **Closed — Satisfied**) | 2026-07-23 | Provider-agnostic framework hardened over the existing prototype: structured FC-13 `ProviderError` taxonomy + retry classification; shared `providerhttp` transport (User-Agent, capped redirects, bounded reads, FC-08 retry/backoff, rate-limit header normalization); `Capabilities()` declaration; explicit `collection.Registry` (duplicate/version/replay-capability validation, startup inspection log); `ReplayDecoder.DecodeStored` deterministic replay; Open-Meteo adapter refactored behind the framework with all contract tests green. New framework tests (`providerhttp`, registry, `ProviderError`, capabilities/replay/rate-limit) pass under `-race`; no DB/API change. Guide: `docs/development/08-provider-adapter-authoring-guide.md`. **DRB 2026-07-23: CONDITIONALLY ACCEPTED** — merits satisfied (no Critical/High; 8/8 scope; acceptance criterion proven live on real PG16; full local gate green), conditional on **TC-05-01** (push WP-05 commit `469560b` + capture six mandatory CI jobs green on that SHA). **TC-05-01 SATISFIED 2026-07-23**: branch `review/wp05-ci-evidence` pushed at exactly `469560b` (remote tip == local HEAD == CI headSha == `469560b3fe9eed8bfecf25b190f93d53cb136069`); CI run **29978249699** (PR #2, event `pull_request`) **success** — all six mandatory jobs green on `469560b`, none skipped; no framework-code change. WP-05 → **READY FOR CONFIRMATORY RE-REVIEW**. Reports: `docs/reviews/work-packages/WP-05-delivery-review.md` (§16), `WP-05-findings.md`, `WP-05-traceability.md`. **Not yet Accepted — WP-06 must not be selected.** **Confirmatory re-review 2026-07-23: ACCEPTED** — the board independently re-verified commit identity + ancestry (`9a6023f` parent of `469560b`), the real remote (`git ls-remote origin` → `refs/heads/review/wp05-ci-evidence` == `refs/pull/2/head` == `469560b3fe9eed8bfecf25b190f93d53cb136069`, == local), and CI run **29978249699** (headSha `469560b`, event `pull_request`) with all six mandatory jobs green (none skipped/cancelled); no post-review framework change (`311a4a7` is docs-only). **TC-05-01 → Closed — Satisfied; WP-05 → Accepted; WP-06 may now be selected** (report §17). TC-05-02/03 (Low) remain Open, non-blocking. |
| 06 | First provider (Open-Meteo) | **Accepted** (confirmatory re-review 2026-07-23; TC-06-01 **Closed — Satisfied**) | 2026-07-23 | Contract-test matrix (`docs/testing/03-contract-testing.md` §1.2) completed over the WP-05 prototype: +2 recorded fixtures (`forecast_localtime_offset`, `forecast_majority_invalid`) and +4 contract tests (BR-PROV-01 timezone conversion; attribution-field capture; >50%-invalid → `failed`/`schema_drift`; auth-failure no-retry) → 10/10 matrix rows green under `-race`. Live-collection acceptance documented (manual smoke procedure + captured-payload evidence under `data/payloads/open-meteo/2026/07/22/`). No production-adapter, DB, API, or CI change (tests/fixtures/docs only). Branch `feature/wp06-open-meteo-provider`. Report: `docs/reviews/work-packages/WP-06-implementation.md`. **DRB 2026-07-23: CONDITIONALLY ACCEPTED** — accepted on technical/architectural merits; sole tracked condition **TC-06-01** (synchronise final-SHA CI evidence in implementation report §15). **TC-06-01 SATISFIED by delivery team 2026-07-23, pending DRB confirmation**: §15 now cites final reviewed commit `da04fe4` (local HEAD == remote tip == CI head SHA == `da04fe49e4daed358e50d06d401c41f781e9e809`) and CI run **29984650522** (PR #3, event `pull_request`) **success** — all six mandatory jobs (`backend-checks`, `backend-integration`, `migrations`, `api-contract`, `security`, `image`) green on `da04fe4`, none skipped/cancelled; no code/test/fixture/CI change. Optional DRB-WP06-002 addressed: test count corrected to the verified 15 discovered top-level tests (`go test -list`). **Confirmatory re-review 2026-07-23: ACCEPTED** — the board independently re-verified commit identity (`da04fe4` preserved in remote branch history), the real remote (`git ls-remote origin` → `refs/heads/feature/wp06-open-meteo-provider` == local HEAD == `af53c5c181b0ff842490cfb29d55f50c69d4559e`, a documentation-only descendant of `da04fe4`), the docs-only closure diff (`da04fe4..af53c5c` touches only this registry + the implementation report), and CI run **29984650522** (headSha `da04fe4`, event `pull_request`) with all six mandatory jobs green (none skipped/cancelled); §15 synchronized to the final SHA and the `(see below)` reference resolved; the TC-06-01 closure commit `af53c5c` is now pushed (durable persistence confirmed); no CI run was triggered for the docs commit, so run `29984650522` on `da04fe4` remains the authoritative implementation CI evidence. No new Critical/High finding. **TC-06-01 → Closed — Satisfied; WP-06 → Accepted; WP-07 may now be selected.** |
| 07 | Second provider (OpenWeather) | **Accepted** (confirmatory re-review 2026-07-23; TC-07-01 **Closed — Satisfied**) | 2026-07-23 | **Implemented 2026-07-23** on branch `feature/wp07-openweather-provider` (base: accepted WP-06 tip on `main` `927106d`). New adapter package `adapters/forecastproviders/openweather` (One Call 3.0, 48-period hourly; `exclude=current,minutely,daily,alerts`; `units=metric`; UTC epoch `dt`; `appid` credential) implementing `ForecastProviderAdapter` + `ReplayDecoder` behind the WP-05 framework. **Daily rate-budget guard** (`budget.go`): UTC-day counter that refuses collection pre-emptively once the day's budget is spent and **pauses on a 429** (honors `Retry-After`, else rests until the next UTC day); clock-injected, thread-safe; wired via `FIQ_PROVIDER_OPENWEATHER_DAILY_BUDGET` (default 1000). Full contract matrix + budget tests (22 tests green under `-race`; 8 fixtures incl. `onecall_401`/`onecall_429`). Composition-root registration in `app.go`; OpenWeather operational configuration seeded **DISABLED** (`OpenWeatherConfigID`, `credential_ref=FIQ_PROVIDER_OPENWEATHER_API_KEY`, staggered `:02`) — ToS gate D-05 is a public-launch gate, so the scheduler generates no OpenWeather slots until an operator activates it. Tomorrow.io swap-path runbook `docs/development/09-tomorrow-io-swap-path.md` + capture script. No domain/service/persistence/API/migration change; `ci.yml` untouched. Report: `docs/reviews/work-packages/WP-07-implementation.md`. Local validation green (gofmt/build/vet/golangci-lint clean; `go test ./...` all `ok`). **CI evidence CAPTURED** — PR #6 → `main` triggered CI run **30011969157** (event `pull_request`) **success** on head SHA `1211878adff0be8ecebd9c0f1bd5312e67add6b5` (`1211878`) with all six mandatory jobs green (`backend-checks`, `backend-integration`, `migrations`, `api-contract`, `security`, `image`), none skipped/cancelled; local == remote tip == CI head SHA. The subsequent docs-only §15 closure commit is a descendant of the CI-verified SHA (no code/CI change), so run `30011969157` on `1211878` remains the authoritative implementation CI evidence. **DRB full review 2026-07-23: CONDITIONALLY ACCEPTED** (report: `docs/reviews/work-packages/WP-07-delivery-review.md`) — reviewed SHA `3b87601` (local == `git ls-remote origin` == CI head), CI run **30012298442** (`pull_request`, head `3b87601`) **success** with all six mandatory jobs green (none skipped/cancelled); 5/5 scope implemented; acceptance proven (429 → pause; budget-exhaustion → pre-emptive refuse); architecture clean; secrets never logged; **no Critical/High**. Findings: **DRB-WP07-001 (Medium)** — the daily budget calls `reserve()` once per fetch but the shared transport retries a daily-quota 429 up to `MaxRetries=5`, so the budget can under-count actual upstream calls (~5× worst case) and a 429 is retried before the pause engages; **DRB-WP07-002 (Low)** — `budget.pause()` uses a timestamp captured before the transport call (early resume); **DRB-WP07-003 (Low, docs)** — report says "22 tests", actual `go test -list` = 21. **Blocking condition TC-07-01** (fix 001 + 002; prove a daily-quota 429 → exactly one upstream request) must be closed **before the OpenWeather provider is activated**; because the config ships **disabled** it does not block merge of the disabled wiring. TC-07-02 (Low, doc count) non-blocking. **Not Accepted — awaiting remediation + confirmatory re-review; only the DRB may mark WP-07 Accepted.** **Team remediation 2026-07-23 (report §24; review report §12): DRB-WP07-001/002/003 RESOLVED** — added an additive, backward-compatible `providerhttp.Config.RetryableOverride` hook + `Response.Attempts` counter; OpenWeather now opts a daily-quota 429 out of transport retry (→ exactly one upstream request + immediate pause) and debits actual upstream attempts from the daily budget (`budget.consume`); pause reads a fresh clock; test count corrected (23 openweather + 2 framework). Full `go test -race ./...` green (openmeteo + existing providerhttp suites unchanged); gofmt/vet/golangci-lint clean. **Remediation CI CAPTURED** — CI run **30013888511** (`pull_request`, PR #6) **success** on head SHA `ce4910f` with all six mandatory jobs green (`backend-integration` cleared a transient Docker Hub image-pull flake on a same-SHA job re-run, no code change); local == remote tip == CI head == `ce4910f`. **READY FOR CONFIRMATORY RE-REVIEW** — the docs-only note of this CI evidence is a descendant of `ce4910f`; pending DRB confirmation to close TC-07-01. **Confirmatory re-review 2026-07-23: ACCEPTED** (report §13) — the board independently re-verified SHA identity (local == `git ls-remote origin` == CI head == `c64c17d`), the docs-only lineage since code commit `860e93b`, the scoped remediation diff (`openweather` + `providerhttp` + tests only), and CI run **30015654361** (`pull_request`, head `c64c17d`) **success** with all six mandatory jobs green (none skipped/cancelled; code SHA `ce4910f` run 30013888511 also green); local gate green; DRB-WP07-001 verified in code (429 opted out of retry via additive `RetryableOverride`; real attempts debited via `Response.Attempts` + `budget.consume`) with proving tests, DRB-WP07-002/003 fixed; no regression (Open-Meteo + existing providerhttp suites unchanged); no new Critical/High. **TC-07-01 → Closed — Satisfied; TC-07-02 → Closed; WP-07 → Accepted. Accepted Implementation SHA `c64c17d`.** PR #6 ready to merge to `main`. Standing note: the OpenWeather config stays seeded **disabled** — operator activation is gated on the ToS review (D-05, a launch gate, not a code gate). WP-09/WP-10 (observation chain) may now be selected in the Provider-2 + observations phase. |
| 08 | Scheduler + collection ops | **Accepted** (DRB full review + re-review 2026-07-23) | 2026-07-23 | Hardened the WP-01-era prototype (bootstrap `4f24fa3`) into the full WP-08 feature set on branch `feature/wp08-scheduler-collection-ops` (base: accepted WP-06 tip `9eda963`): watchdog/missed-slot detection + `scheduler_lag_seconds`; graceful in-flight drain (loop-detached job ctx + bounded deadline; `serve` waits before pool close); raw-payload replay (FC-14) `POST /admin/collections/{id}/replay` (SHA-256 verify → quarantine-on-mismatch → decode via current adapter → dedup'd collection); manual-trigger 429 rate guard. New config `FIQ_WORKER_JOB_TIMEOUT`/`FIQ_SCHEDULER_DRAIN_TIMEOUT`/`FIQ_SCHEDULER_MISSED_THRESHOLD`; OpenAPI +1 path. Reports: `docs/reviews/work-packages/WP-08-implementation.md`, `WP-08-delivery-review.md`. **DRB full review 2026-07-23 (SHA `7e29571`, CI run 29998226061 six jobs green): CHANGES REQUIRED** — finding **DRB-WP08-001 (High)**: replay wrote a `success` collection that shadowed the original in `LatestSuccessful`, regressing `/forecasts/latest` (replay row owned zero snapshots under ON CONFLICT DO NOTHING). **Remediated** in `daee1e1`: replay recorded as `deduplicated` (excluded from success dedup index + LatestSuccessful) while still landing new-key snapshots; regression test `TestReplayDoesNotShadowLatestForecast` added. **Re-review + confirmatory 2026-07-23: ACCEPTED** — SHA identity verified (local == `git ls-remote origin` == CI head == `daee1e1bd07718ce073ba0c11d5e02cd8fa9432c`), CI run **29998847727** (`pull_request`, head `daee1e1`) **success** with all six mandatory jobs green (none skipped/cancelled); DRB-WP08-001 CLOSED; scope clean (no migration/CI-config change). DRB-WP08-002 (idle watchdog count query) remains Open — Informational, non-blocking. **Accepted Implementation SHA `daee1e1`.** PR #4 open, ready to merge to `main`. |
| 09 | Observation source adapter | **Accepted** (DRB full review 2026-07-23) | 2026-07-23 | **Selected 2026-07-23** following WP-07 acceptance (PR #6 merged to `main` as `7101eea`); **implemented 2026-07-23** on branch `feature/wp09-observation-adapter` (base `275114e`). New observation domain (`internal/collection/domain/observation.go`: `Observation` root, `ObservationType`/`QualityFlag` enums, OC-04 `RangeReasons()` reusing shared range constants, `DiffersFrom()` correction primitive with per-variable ε — temp 0.1°C/precip 0.05mm/…); observation port (`ports.ObservationSourceAdapter` + request/result, reusing `Outcome`/`RateLimit`); Open-Meteo Historical adapter (`adapters/observationsources/openmeteo`: 2 h window via `start_hour`/`end_hour`, `timezone=UTC`, schema `openmeteo-historical-v1`, reanalysis-default provenance [ADR-003/A-4, configurable], OC-04 range→suspect [rows kept], WMO condition map, `observed_at ≤ window-end` invariant) over the shared `providerhttp` transport (unchanged). 15 tests green under `-race` (11 adapter contract + 4 domain); 5 fixtures under `test/fixtures/openmeteo-historical/`; capture script added. **Deferred to WP-10**: storage tx + dedup, supersession UPDATE, `observation.collected`/`corrected` events, freshness gauge, `:05` scheduler slots, composition-root wiring, and the `observations`/`observation_type`/`quality_flag` migration. No DB/API/migration/CI change; no wiring (no consumer yet). Report: `docs/reviews/work-packages/WP-09-implementation.md`. Local gate green (gofmt/build/vet/golangci-lint; `go test -race ./...` all `ok`). **DR-04** recorded (observation fixtures under `openmeteo-historical/`, doc §1.1 updated). **CI evidence CAPTURED** — PR #7 → `main` triggered CI run **30023733403** (`pull_request`) **success** on head SHA `ffc47876c1199b87226c2a962c6ecd676050a121` (`ffc4787`) with all six mandatory jobs green (none skipped/cancelled); local == remote tip == CI head SHA. **Not Accepted — awaiting Delivery Review Board.** **DRB full review 2026-07-23: ACCEPTED** (report: `docs/reviews/work-packages/WP-09-delivery-review.md`) — reviewed SHA `1bc42e8` (local == `git ls-remote origin` == CI head; docs-only lineage since code tip `8598f47`), CI run **30024064988** (`pull_request`, head `1bc42e8`) **success** with all six mandatory jobs green (none skipped/cancelled); local gate green; 5/5 scope; architecture clean; keyless + payload-free (ADR-025); domain §2.7 invariants honored; no scope creep (no storage/events/wiring); **no Critical/High/Medium**. Sole finding **DRB-WP09-001 (Low, docs)**: `ObservationRequest` struct comment writes `[WindowStart, WindowEnd)` but behavior + field comment are inclusive — non-blocking, fold into WP-10. **WP-09 → Accepted. Accepted Implementation SHA `1bc42e8`.** PR #7 ready to merge to `main`. **WP-10 (observation collection) may now be selected** (consumes this adapter + `Observation.DiffersFrom`). |
| 10 | Observation collection | **Accepted** (DRB full review 2026-07-23) | 2026-07-23 | **Selected 2026-07-23** following WP-09 acceptance (PR #7 merged to `main` as `e80a51e`). Rationale: WP-10 completes the **Provider-2 + observations** phase; both hard dependencies are **Accepted** — WP-09 (observation source adapter + `Observation.DiffersFrom`) and WP-08 (scheduler: slot generation, SKIP-LOCKED claims, leases, watchdog, drain). Scope (per `05-implementation-work-packages.md` §WP-10): scheduled observation pipeline — observation slots at **:05** per active location; the `observations`/`observation_type`/`quality_flag` **migration** (deferred by `20260801000001`); storage tx with `ON CONFLICT (source, location_id, observed_at) DO NOTHING` dedup; correction detection via re-fetch value-diff → new `corrected` row + `superseded_observation_id` UPDATE on the old row (the one permitted mutation); `observation.collected`/`observation.corrected` event emission (shapes frozen in `platform/events`); `observation_freshness_age_seconds` gauge; composition-root wiring of the WP-09 adapter + an observation dispatcher/collector. Tests: window-backfill dedup, correction → superseded link + event, suspect exclusion downstream. Acceptance: 48 h unattended observation collection in dev. Estimate 2–3 d; commit slices (1) collection job, (2) correction flow + events. Also folds in **DRB-WP09-001** (Low: `ObservationRequest` window comment inclusivity). **Implemented 2026-07-23** on branch `feature/wp10-observation-collection` (base `9900814`): migration `20260801000007_create_observations` (enums; monthly-partitioned `observations`; **partial** live-row dedup index `WHERE superseded_observation_id IS NULL` — DR-05; supersession-only immutability trigger); `ObservationRepository` port + `observationpg`; `collection.ObserveService` (single-tx new/correction/dedup classification, supersede-then-insert, `observation.collected`/`corrected` events, freshness gauge); scheduler `:05` `observation_collection` slots owned by the seeded Open-Meteo config (user-confirmed; `job_type` discriminates), `Router` + `ObservationDispatcher`; app.go wiring. Scheduler routing/window unit tests + real-PG integration (dedup, correction→supersede+event, suspect stored). **DR-05** resolved (table-design §3 corrected). No API change; matching on corrections is WP-11+. Report: `docs/reviews/work-packages/WP-10-implementation.md`. Local gate green (gofmt/build/vet/golangci-lint; `go test -race ./...` unit `ok`; integration compiles). **CI evidence CAPTURED** — PR #8 → `main` triggered CI run **30031945500** (`pull_request`) **success** on head SHA `b42e937800ff61a0c3f0d2d91d39ba54ed210b95` (`b42e937`) with all six mandatory jobs green (none skipped/cancelled; `migrations` applied `20260801000007`, `backend-integration` ran the observation pipeline against real PG16); local == remote tip == CI head SHA. **Not Accepted — awaiting Delivery Review Board.** **DRB full review 2026-07-23: ACCEPTED** (report: `docs/reviews/work-packages/WP-10-delivery-review.md`) — reviewed SHA `0f16adc` (local == `git ls-remote origin` == CI head; docs-only lineage since code tip `b42e937`), CI run **30031945500** (`pull_request`, head `b42e937`) **success** with all six mandatory jobs green (`migrations` + real-PG `backend-integration` exercised the migration, partial dedup index, supersession trigger, and correction cascade); local gate green; 4/4 scope + acceptance; architecture clean; keyless + payload-free (ADR-025); parameterized SQL; domain §2.7 invariants enforced at the DB. Adversarial review found **no** defect on the high-risk mechanics (supersede-before-insert ordering, partial-index ON CONFLICT, trigger, concurrency, month-boundary partitions, window semantics). **No Critical/High/Medium/Low finding.** DR-05 resolved; DRB-WP09-001 closed. **WP-10 → Accepted. Accepted Implementation SHA `0f16adc`.** PR #8 ready to merge to `main`. **Provider-2 + observations phase complete; WP-11 (Matching Engine) may now be selected** (consumes snapshots + observations + the `observation.corrected` event). |
| 11 | Matching engine | **Accepted** (DRB full review 2026-07-23) | 2026-07-23 | **Selected 2026-07-23** following WP-10 acceptance (PR #8 merged to `main` as `3c4ac83`). Rationale: the **Provider-2 + observations** phase is complete and WP-11 opens the **Analysis** phase; both hard dependencies are **Accepted** — WP-10 (observations + `observation.corrected` event) and WP-08 (scheduler, for the `analysis_batch` job). Scope (per `05-implementation-work-packages.md` §WP-11; ADR-014; `docs/workflows/03-matching.md`): deterministic exact-hour UTC matching (BR-MATCH-01..06) as a chunked (5K) analysis batch; total-order candidate selection (corrected preference → provenance rank → |Δ to top-of-hour| → id); uniqueness-guarded pair inserts `(forecast_snapshot_id, observation_id)`; rematch on `observation.corrected` (new pairs only, old retained); suspect-observation exclusion; matching backlog gauge; the `matched_evaluations` **migration** (analysis-module table, table-design §4). Tests: selection determinism (property: shuffled candidates → same choice), rematch creates new pairs, suspect exclusion, idempotent re-run. Acceptance: matching batch over a seeded month < 2 min; all BR-MATCH rules tested. Estimate 3–4 d; commit slices (1) batch + selection, (2) correction rematch, (3) properties. WP-12 (pair-level evaluation) is the natural follow-on. **Implemented 2026-07-23** on branch `feature/wp11-matching-engine` (base `fdbc9b6`): new `internal/analysis` module (`domain.SelectCandidate` total-order deterministic selection — corrected → provenance rank → nearest-hour → id; `MatchService` chunked (5K) batch over `[now−30d, now−2h]` with per-chunk tx + rematch pass for superseded observations); migration `20260801000008_create_analysis` (`matched_evaluations`, pair uniqueness, immutability trigger); `analysispg` (unmatched NOT-EXISTS scan, exact-hour candidates, `ON CONFLICT DO NOTHING` inserts, rematch-target join, backlog count); scheduler `analysis_batch` `:10`/`:40` global slots (Open-Meteo-config-owned) + `AnalysisDispatcher` + `Router`; `matching_pairs_created_total`/`matching_backlog` metrics; app.go wiring. Tests: 500-permutation determinism property (unit) + real-PG integration (match, suspect exclusion, idempotent, correction→rematch new-pair-old-retained). No API change; metrics/rankings are WP-12+. Report: `docs/reviews/work-packages/WP-11-implementation.md`. Local gate green (gofmt/build/vet/golangci-lint; `go test -race ./...` unit `ok`; integration compiles). **CI evidence CAPTURED** — PR #9 → `main` triggered CI run **30038659743** (`pull_request`) **success** on head SHA `674dfddf1bc62005d582ee05eeedb6c4aab510b2` (`674dfdd`) with all six mandatory jobs green (none skipped/cancelled; `migrations` applied `20260801000008`, `backend-integration` ran the matching batch/rematch tests against real PG16; two earlier integration failures were test-harness-only bugs fixed in `0a190bc`/`674dfdd`, product code unchanged); local == remote tip == CI head SHA. **Not Accepted — awaiting Delivery Review Board.** **DRB full review 2026-07-23: ACCEPTED** (report: `docs/reviews/work-packages/WP-11-delivery-review.md`) — reviewed SHA `f02f720` (local == `git ls-remote origin` == CI head; docs-only lineage since code+test tip `674dfdd`), CI run **30039043834** (`pull_request`, head `f02f720`) **success** with all six mandatory jobs green (code+test tip `674dfdd` run 30038659743 also green); local gate green incl. the 500-permutation determinism property; 4/4 scope + acceptance; new analysis module with correct dependency direction; append-only rematch + immutable pairs; parameterized SQL. Adversarial review found **no** defect on the high-risk mechanics (keyset-advance, strict-total-order determinism, rematch NOT-EXISTS/chain/no-fan-out, global-slot uniqueness, window boundary, tx isolation). **No Critical/High/Medium/Low finding.** **WP-11 → Accepted. Accepted Implementation SHA `f02f720`.** PR #9 ready to merge to `main`. **WP-12 (Pair-Level Evaluation) may now be selected** (consumes `matched_evaluations`). |
| 12–27 | (remaining) | Not Started | 2026-07-22 | |

---

## Recorded discrepancies

Documentation-vs-documentation conflicts discovered during implementation, with resolutions (master prompt: "record the discrepancy and resolve it before materially extending the affected behaviour").

| # | Discrepancy | Resolution | Affected docs | Packages impacted |
|---|-------------|------------|---------------|-------------------|
| DR-01 | Location timezone mutability: domain architecture §2.3 lists timezone as **immutable** (Mutable: name, status, updated_at); `docs/api/00-api-requirements.md` §4.1 listed PUT as "(name, timezone, status)"; UI design spec edit form includes timezone; security matrix lists "name/timezone/status" | Domain architecture authoritative (Phase 1 aggregate design; timezone derives from immutable coordinates; mutation would re-bucket historical display data). Implementation: PUT accepts name only. API requirements doc corrected 2026-07-22. | `docs/api/00-api-requirements.md` (corrected); `docs/ui/02-ui-design-specification.md` §S-12 (edit form should render timezone read-only — flagged for WP-21); `docs/security/01-ui-authorization-matrix.md` S-12 row (flagged for WP-19 review) | WP-21 (UI form), WP-19 (matrix) |
| DR-02 | Contract-testing doc §1.1 illustrative fixture name `forecast_success_v1_edge.json` differs from the committed `forecast_edge_nulls.json` | §1.2 matrix uses generic labels; committed names authoritative (existing tests reference them). No rename performed; recorded for documentation consistency. Discovered during WP-06. | `docs/testing/03-contract-testing.md` §1.1 (illustrative only) | None (documentation-only) |
| DR-03 | WP-03 work-package summary lists **DB changes: None (schema from WP-02)**, but WP-02 never created the `users`/`api_keys` tables — the audit migration header explicitly defers the `users` table and the `audit_events.user_id` FK to WP-03. | Table-design doc §2 + the audit migration header are authoritative: WP-03 owns the identity tables. WP-03 adds migration `20260801000006_create_identity` (`user_role` enum, `users`, `api_keys`, audit `user_id` FK); `export_jobs` (GDPR) remains deferred to WP-19 per WP-03 scope. Discovered during WP-03. | `docs/planning/05-implementation-work-packages.md` §WP-03 ("DB changes" line is inaccurate) | WP-03 (adds a migration) |
| DR-04 | Contract-testing doc §1.1 illustrative tree listed the observation fixtures (`historical_*`) under `test/fixtures/openmeteo/` (shared with forecast fixtures). | Committed under a dedicated `test/fixtures/openmeteo-historical/` directory to keep forecast and observation fixtures separate (distinct adapters/schemas). §1.1 updated to match the committed layout. Discovered during WP-09. | `docs/testing/03-contract-testing.md` §1.1 (illustrative only; corrected) | None (documentation-only) |
| DR-05 | `docs/data/03-table-design.md` §3 declared `observations_dedup` as a plain unique index on (source, location_id, observed_at). That is incompatible with the correction model (workflow §4 / domain §2.7): a correction inserts a NEW row with the SAME key and marks the old row superseded, which a plain unique index rejects. | The dedup index must be **partial** — `WHERE superseded_observation_id IS NULL` — so exactly one live row exists per (source, location, hour) while superseded history is retained. Implemented in migration `20260801000007`; table-design §3 DDL corrected to match. Discovered during WP-10. | `docs/data/03-table-design.md` §3 (corrected) | WP-10 (migration) |

---

## WP-04 Delivery Review Board outcome (2026-07-22)

Decision: **CHANGES REQUIRED** (report: `docs/reviews/work-packages/WP-04-delivery-review.md`).

| Finding | Severity | Summary | Remediation target |
|---------|----------|---------|--------------------|
| DRB-WP04-001 | High | BR-LOC-01 dedup race: concurrent creates bypass proximity check (reproduced: 6 parallel POSTs → 2 rows); no concurrency test | Serialize create tx (advisory lock or SERIALIZABLE+retry) + concurrency integration test |
| DRB-WP04-002 | Medium | Exact-0.05° boundary fp-fragile (live pair rejected at 0.04999999999999716°) | Epsilon/precision-tolerant comparison + multi-coordinate boundary tests |
| DRB-WP04-003 | Medium | `override_reason` not mandatory when `allow_near_duplicate` set | 422 on empty reason + tests |
| DRB-WP04-004 | Medium | Reserved `archived` settable via PATCH; no transition validation | Restrict to active\|disabled (or ADR) + transition tests |
| DRB-WP04-005 | Medium | `docs/api/05-endpoint-catalog.md` still lists PUT timezone; README endpoint list stale | Doc corrections |
| DRB-WP04-006 | Low | Repository UPDATE writes immutable timezone column | Remove column from statement |
| DRB-WP04-007 | Low | Malformed `active` query param silently coerced | 422 on invalid boolean |

Tracked conditions: TC-01 Idempotency-Key (→ WP-15, this registry's deferral accepted); TC-02 optimistic concurrency (deferred); TC-03 dev-token seam replacement (WP-03/19) + DR-01 UI/matrix follow-ups (WP-21/19); TC-04 CI green on pushed branch + testcontainers run (review env had no Docker).

## WP-04 Delivery Review Board **Re-Review** outcome (2026-07-23)

Decision: **BLOCKED** (report: `docs/reviews/work-packages/WP-04-delivery-re-review.md`). All five original findings independently verified **RESOLVED** (High concurrency finding proven fixed against real PostgreSQL 16; four Medium findings closed with executed unit + integration tests and matching OpenAPI/docs). No remediation-caused Critical/High regression. Blocked purely on evidence:

- **TC-04 NOT SATISFIED / BLOCKED BY EXTERNAL ACCESS** — `git ls-remote origin` fails authentication; no distinct remediation branch (all on `main`); local `origin/main` moved to HEAD unverifiably; GitHub Actions results not inspectable. Local checks do not substitute for pushed-branch CI.
- **`make test-integration` red** — DRB-WP04-RR-001 (Medium, **pre-existing** test-assertion bug: string `"true"` vs boolean `true` at `location_test.go:68`; production correct) + DRB-WP04-RR-002 (Low, flaky per-test containers).

Next action (implementation team): push the branch with working credentials and capture CI for `fc72f08`; fix RR-001 and stabilise the suite; then re-convene for a short confirmatory re-review. **WP-05 must not be selected until WP-04 is Accepted.**

## WP-04 team final remediation (2026-07-23)

Status: **PARTIALLY COMPLETE** (report addendum: `docs/reviews/work-packages/WP-04-delivery-re-review.md` §A1–A6). The implementation team addressed the two evidence blockers within strict scope (test-only + test-harness; no production, migration, OpenAPI, or CI changes):

- **DRB-WP04-RR-001 — RESOLVED (test-only).** `test/integration/location_test.go` now type-asserts `allow_near_duplicate` as a JSON boolean (`bool` + `true`) instead of the string `"true"`. Production audit payload unchanged. Commit `bf694a0`.
- **DRB-WP04-RR-002 — RESOLVED (test-harness).** Single package-wide PostgreSQL container via `TestMain`; each test gets a fresh migrated database (`it_<pid>_<counter>`) dropped `WITH (FORCE)`. Per-test container churn eliminated; isolation preserved; no retries/skips/weakened assertions. 5 consecutive local green runs (~40 s → ~7 s). Commit `b7f2479`.
- **TC-04 — PARTIALLY SATISFIED.** Branch `fix/wp04-final-review` (base `33ad0ab`, tip `1fa9105`) pushed via SSH; local == remote SHA verified; PR #1 triggered CI run `29945014559` (`pull_request`, headSha `1fa9105`). **`backend-integration` went red→green** (failure on `main` `fc72f08` → success on `1fa9105`), verifying RR-001+RR-002 in CI. `api-contract`, `migrations`, `image` also green.
- **Out of scope (deferred, separate work item).** Two mandatory but **pre-existing, unrelated** CI jobs remain red on the branch (they fail on `main` independently of WP-04): `backend-checks` (`govulncheck` flags Go 1.23.x stdlib CVEs GO-2025-4007/4008 + `golang.org/x/net@v0.25.0` GO-2025-3595) and `security` (`gitleaks` HTTP 403 `Resource not accessible by integration` — a `pull_request`-event token-permission quirk, not a detected secret). By explicit scope decision these were left unchanged; their remediation (Go toolchain/dependency bump; CI `permissions:` block) is a separate task.

Next action: raise the two pre-existing CI failures as a separate maintenance task, then re-convene the board for a short confirmatory re-review. **Only the Delivery Review Board may mark WP-04 Accepted. WP-05 must not be selected until WP-04 is Accepted.**

## WP-04 mandatory CI gate remediation (2026-07-23)

Status: **READY FOR CONFIRMATORY RE-REVIEW** (report addendum: `docs/reviews/work-packages/WP-04-delivery-re-review.md` §B). The two mandatory but pre-existing/unrelated CI jobs deferred by the team final remediation were fixed within strict scope (build/dependency + CI config only; no WP-04 production, migration, or OpenAPI changes):

- **CI-WP04-001 — `backend-checks` RESOLVED (code/dependency defect).** `govulncheck` flagged real Go 1.23.x stdlib CVEs plus outdated deps. Fixed by moving the module to `go 1.25.0` + `toolchain go1.25.12`, upgrading `pgx/v5 v5.6.0→v5.9.2`, `x/text v0.15.0→v0.39.0`, `x/net v0.25.0→v0.56.0` (`go mod tidy`), bumping the `Dockerfile` base to `golang:1.25-alpine`, and updating `ci.yml` (`go-version 1.25.12`; `govulncheck@v1.6.0`; `golangci-lint install-mode: goinstall` for v1.64.8-on-Go-1.25). No jobs disabled, no suppressions. One **uncalled** advisory (`GO-2026-5932`, `x/crypto@v0.53.0`, no fix) remains informational and does not fail the gate. Commit `542c808`.
- **CI-WP04-002 — `security` RESOLVED (CI-config defect).** `gitleaks-action@v2` needs `pull-requests: read` to list PR commits on `pull_request` events; the default token (`contents: read` only) caused HTTP 403 `Resource not accessible by integration` — not a detected secret. Fixed with a least-privilege job-scoped `permissions:` block (`contents: read` + `pull-requests: read`). Detection unchanged. Commit `b277fba`.
- **CI evidence.** Run `29952013546` (`pull_request`, headSha `b277fba`): **all six mandatory jobs green** (`backend-checks`, `security`, `backend-integration`, `migrations`, `api-contract`, `image`). Prior baseline `701a0ed` run `29946041618` had `backend-checks`+`security` red. <https://github.com/od3n/forecastiq/actions/runs/29952013546>

Next action: re-convene the Delivery Review Board for the short confirmatory re-review. **Only the board may mark WP-04 Accepted. WP-05 must not be selected until WP-04 is Accepted.**

## WP-04 Delivery Review Board confirmatory re-review outcome (2026-07-23)

Decision: **ACCEPTED** (report: `docs/reviews/work-packages/WP-04-delivery-re-review.md`). The board independently verified: commit `15b8faa` (local HEAD == `git ls-remote origin` == CI headSha); CI run **29952834878** (`pull_request`, headSha `15b8faa`) **completed success** with all six mandatory jobs green (`backend-checks`, `api-contract`, `image`, `backend-integration`, `migrations`, `security`) — no required job skipped or cancelled (all jobs unconditional/blocking in `ci.yml`). DRB-WP04-001..005 remain **RESOLVED** (remediation code artifacts present in-tree; no regression); RR-001/RR-002 remain **RESOLVED**; TC-04 **SATISFIED** (branch pushed, remote SHA verified, CI evidence, commit identity confirmed). `15b8faa` is documentation-only over the code-fix tip `b277fba`, so the CI surface is unchanged. Documentation is consistent; local build + catalog unit tests pass. No new Critical/High issue. **WP-04 dependency contracts (LocationManager, active-location queries) are stable and hardened — WP-05 may now depend on WP-04.**

**Package transition:** READY FOR CONFIRMATORY RE-REVIEW → **Accepted**. WP-05 may be selected for implementation (not started by the board).

## WP-05 Delivery Review Board outcome (2026-07-23)

Decision: **CONDITIONALLY ACCEPTED** (report: `docs/reviews/work-packages/WP-05-delivery-review.md`; findings: `WP-05-findings.md`; matrices: `WP-05-traceability.md`). First independent review of WP-05. Dependency gate satisfied (WP-04 **Accepted**). Reviewed commit `469560b` on the accepted WP-04 tip `15b8faa` (via `9a6023f`).

**Merits (accepted):** 8/8 approved scope items implemented; provider-agnostic framework (shared `providerhttp` transport, FC-13 `ProviderError` taxonomy, `Capabilities()`, explicit `Registry` with startup validation + `provider.registered` log, deterministic `DecodeStored` replay, Open-Meteo refactored behind the framework) is architecturally clean with correct dependency direction and no DB/API/migration change. The board **executed the full quality gate locally with Docker present**: `go build`, `go vet`, `golangci-lint` (zero findings), `go test -race` (all packages green), and the testcontainers integration suite against **real PostgreSQL 16 (green, 6.4 s)** — including `TestCollectionIdempotency`, which proves the WP-05 acceptance criterion (full pipeline green with a fake provider; idempotent re-execution). **No Critical or High finding.**

**Findings:**

| Finding | Severity | Summary | Condition |
|---------|----------|---------|-----------|
| DRB-WP05-001 | Medium (evidence) | WP-05 commit `469560b` unpushed; no pushed-branch CI on the WP-05 SHA (mandatory gate) | TC-05-01 (blocking) |
| DRB-WP05-002 | Low | Pipeline-level non-success classification (partial/failed/timeout/rate_limited) not driven through `CollectService.Collect` | TC-05-02 |
| DRB-WP05-003 | Low | `ports.VerifyChecksum` has no direct unit test | TC-05-03 |

Tracked conditions: **TC-05-01 (blocking)** — push `469560b` (and `9a6023f`) and capture all six mandatory CI jobs green on that exact SHA; on satisfaction this converts to a clean **ACCEPTED** with no framework code change. TC-05-02/TC-05-03 opportunistic (non-blocking).

Next action (implementation team): run a WP-05 remediation pass for the identified findings (satisfy TC-05-01; optionally close the two Lows), then re-convene the board for a short confirmatory re-review. **Only the Delivery Review Board may mark WP-05 Accepted. WP-06 must not be selected until WP-05 is Accepted.**

**TC-05-01 closure (2026-07-23, closure team).** Branch `review/wp05-ci-evidence` pushed at exactly `469560b` (remote tip == local HEAD == CI headSha == `469560b3fe9eed8bfecf25b190f93d53cb136069`; parent `9a6023f` contained). CI run **29978249699** (workflow `CI`, event `pull_request`, PR #2 → `main`) completed **success** with all six mandatory jobs green on `469560b`: `backend-checks`, `backend-integration`, `migrations`, `api-contract`, `security`, `image` (none skipped/neutral/cancelled). **No framework-code change.** DRB-WP05-001 **Resolved**; **TC-05-01 SATISFIED**. WP-05 transitions to **READY FOR CONFIRMATORY RE-REVIEW** (not Accepted — DRB transition only). TC-05-02 / TC-05-03 (Low) remain Open.

**Confirmatory re-review (2026-07-23, Delivery Review Board).** Decision: **ACCEPTED** (report: `docs/reviews/work-packages/WP-05-delivery-review.md` §17). The board independently re-verified: both commits exist and `9a6023f` is the parent of `469560b`; `git ls-remote origin` shows `refs/heads/review/wp05-ci-evidence` == `refs/pull/2/head` == local == `469560b3fe9eed8bfecf25b190f93d53cb136069` (history preserved); CI run **29978249699** (`pull_request`, headSha `469560b`) **success** with all six mandatory jobs green (`backend-checks`, `backend-integration`, `migrations`, `api-contract`, `security`, `image`) — none skipped/cancelled, no `continue-on-error`; the only post-review commit (`311a4a7`) is documentation-only and off the reviewed branch, so the CI-tested framework commit remains exactly `469560b`. No new Critical/High finding. **TC-05-01 → Closed — Satisfied; DRB-WP05-001 → Closed; WP-05 → Accepted.** WP-06 may now be selected in a separate action. TC-05-02 / TC-05-03 (Low) remain Open, non-blocking.

## Deferred items recorded during WP-04

| Item | Rationale | Revisit |
|------|-----------|---------|
| Idempotency-Key infrastructure for POST /locations | No `idempotency_keys` table in the approved 18-table schema; cross-cutting concern spanning all mutable POSTs; WP-04 acceptance (BR-LOC-01..03) does not require it; snapshot/collection dedup makes re-execution harmless for the pipeline | WP-15 or a dedicated cross-cutting package |
| Optimistic concurrency on location update | Master prompt conditions it on "if approved"; no ADR or domain-architecture section approves it; single-operator MVP with DB unique constraints suffices | If multi-operator admin emerges |
