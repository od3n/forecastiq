# gitleaks configuration — secret scanning gate (CI + pre-commit).
# Extends the default ruleset; allowlists documented synthetic values used
# in fixtures, examples, and local dev so the gate stays noise-free.
title = "ForecastIQ gitleaks config"

[extend]
useDefault = true

[allowlist]
description = "Documented synthetic / example values (not real secrets)"
paths = [
  '''(^|/)\.env\.example$''',
  '''(^|/)test/fixtures/''',
  '''(^|/)docs/''',
  '''(^|/)\.gitleaks\.toml$''',
  '''go\.(mod|sum)$''',
  # Sanitizer tests intentionally contain secret-shaped fixtures to prove redaction.
  '''internal/platform/logging/sanitize_test\.go$''',
]
regexes = [
  '''dev-admin-token''',
  '''forecastiq:forecastiq''',
  '''EXAMPLE[_-]?KEY''',
  '''test-key''',
  # Public production hostnames (never secrets) — the generic-api-key rule
  # matches the dotted default value in deploy/cotenant/cf_setup.py.
  '''forecastiq(-api)?\.od3n\.com''',
]
